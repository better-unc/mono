{
  "name": "gitbruv",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "dev:web": "turbo dev --filter=@gitbruv/web",
    "dev:worker": "turbo dev --filter=@gitbruv/worker",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:generate": "turbo db:generate --filter=@gitbruv/db",
    "db:migrate": "turbo db:migrate --filter=@gitbruv/db",
    "db:push": "turbo db:push --filter=@gitbruv/db",
    "db:studio": "turbo db:studio --filter=@gitbruv/db",
    "cleanup:accounts": "turbo cleanup:accounts --filter=@gitbruv/db"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5"
  },
  "packageManager": "bun@1.1.38"
}
